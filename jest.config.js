export default {
  // Diz ao Jest para transformar o código para que ele entenda
  transform: {},
  // Ambiente de execução (node para backend)
  testEnvironment: 'node',
  // Extensões de arquivo que ele vai procurar
  moduleFileExtensions: ['js', 'mjs', 'json'],
};
